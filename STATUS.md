# Release status

Current status: **Candidate — clean-runtime qualified, promotion pending.** The pipeline package, 42 offline unit tests, `MODEL_CARD.md`, and the standalone `E2E` tutorial `tutorials/bioclip2_biodiversity_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) pass static validation and GitHub Actions. Exact candidate commit `22f2854` / notebook blob `60e7655f` also passed all 15 code cells in a fresh Kaggle Tesla T4 container with an empty cache and snapshot, including the complete 1.71 GB download-and-verification path. Full evidence is recorded in `docs/release-verification.md`. The tutorial remains non-release-grade until the maintainer explicitly approves promotion.

No licence or remote-code gate is open for DIMER hosting (MIT weights, plain safetensors into open_clip's own module). The operational questions the fleet inventory records for this row — GPU budget and the domain-fine-tuning recipe — are addressed by evidence in `MODEL_CARD.md` (*DIMER deployment notes*).
